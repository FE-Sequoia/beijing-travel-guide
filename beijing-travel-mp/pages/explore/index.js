const { getCategories, getPlaces } = require('../../utils/data');
Page({ data: { categories: [], places: [], active: '', keyword: '', sort: 'recommendation', sortOptions: ['优先推荐', '按名称'] }, onLoad() { this.setData({ categories: getCategories() }); this.filter(); }, onShow() { const pending = getApp().globalData.exploreCategory; if (pending !== undefined) { getApp().globalData.exploreCategory = undefined; this.setData({ active: pending }); } if (this.data.categories.length) this.filter(); }, onInput(e) { this.setData({ keyword: e.detail.value }); this.filter(); }, onCategory(e) { this.setData({ active: e.currentTarget.dataset.id }); this.filter(); }, onSort(e) { this.setData({ sort: Number(e.detail.value) === 1 ? 'name' : 'recommendation' }); this.filter(); }, filter() { this.setData({ places: getPlaces({ categoryId: this.data.active, keyword: this.data.keyword, sort: this.data.sort }) }); }, onPlace(e) { wx.navigateTo({ url: '/pages/place-detail/index?id=' + e.detail.id }); }, clear() { this.setData({ keyword: '', active: '' }); this.filter(); } });
