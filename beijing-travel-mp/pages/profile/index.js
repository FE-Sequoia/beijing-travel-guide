const { getPlaces } = require('../../utils/data'); const storage = require('../../utils/storage');
Page({ data: { favorites: [], history: [] }, onShow() { this.refresh(); }, refresh() { const all = getPlaces(); const resolve = (ids) => ids.map((id) => all.find((p) => p.id === id)).filter(Boolean); this.setData({ favorites: resolve(storage.getFavorites()), history: resolve(storage.getHistory()) }); }, onPlace(e) { wx.navigateTo({ url: '/pages/place-detail/index?id=' + e.detail.id }); }, clearFavorites() { wx.showModal({ title: '清空所有收藏？', content: '清空后无法恢复。', success: (res) => { if (res.confirm) { storage.clearFavorites(); this.refresh(); } } }); }, clearHistory() { wx.showModal({ title: '清空阅读历史？', content: '清空后无法恢复。', success: (res) => { if (res.confirm) { storage.clearHistory(); this.refresh(); } } }); } });
